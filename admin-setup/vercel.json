{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/((?!auth-recovery\\.html$).*)",
      "destination": "/index.html"
    }
  ]
}
