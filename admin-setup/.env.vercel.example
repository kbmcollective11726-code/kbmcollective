# Values to set in Vercel (Dashboard → admin-setup → Settings → Environment Variables)
# Or run from admin-setup: npm run vercel:env (reads from ../.env or .env)
#
# Production + Preview:
#   VITE_SUPABASE_URL     = https://YOUR_PROJECT.supabase.co
#   VITE_SUPABASE_ANON_KEY = your_anon_key
