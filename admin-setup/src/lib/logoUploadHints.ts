/** Shown next to vendor / solution-provider booth logo upload in cadmin. */
export const VENDOR_BOOTH_LOGO_HINT =
  'Best: PNG with a transparent background. Square or wide logos both work. JPEG is OK only if the logo is already on a white background — do not use JPG with a black or solid-color fill behind the artwork. About 512 px or wider on the long edge is enough; larger files are resized automatically.';

/** Shown next to sponsor logo upload in each sponsor editor. */
export const SPONSOR_LOGO_UPLOAD_HINT =
  'Best: PNG with a transparent background. For sponsor strips in the app, use a wide 3:1 banner (e.g. 1200×400 px) — see the blue note at the top of this page. JPEG only if the logo is on a white background.';

/** Blue callout at top of Event Sponsors page. */
export const SPONSOR_LOGO_CALLOUT_TITLE = 'Logo file guidelines';
export const SPONSOR_LOGO_CALLOUT_BODY =
  'Use a PNG with a transparent background when possible. For wide “Mobile app sponsored by” strips, use a 3:1 horizontal image (recommended 1200×400 px). JPEG is OK only if the logo is already on a white background — black or solid fills in the file will show in the app. Re-upload to fix logos that look wrong.';

export const LOGO_FILE_ACCEPT = 'image/png,image/jpeg,image/jpg,image/webp';
