/** Info hero on the app — 16:10 full-bleed (May 2026 layout). */

export const INFO_BANNER_DISPLAY_ASPECT = 16 / 10;

export const EVENT_BANNER_ASPECT = INFO_BANNER_DISPLAY_ASPECT;

export const EVENT_BANNER_WIDTH = 1600;
export const EVENT_BANNER_HEIGHT = 1000;

export const EVENT_BANNER_LETTERBOX_BG = '#0c1f3d';

export const EVENT_BANNER_SIZE_LABEL = `${EVENT_BANNER_WIDTH}×${EVENT_BANNER_HEIGHT} px (16:10)`;

export const EVENT_BANNER_HINT =
  'Info banner — upload a wide JPG/PNG (recommended 1600×1000 or 1200×600). ' +
  'We keep your aspect ratio; the app sizes the hero to match so the full artwork shows.';

export const BANNER_FILE_ACCEPT = 'image/png,image/jpeg,image/jpg,image/webp';
